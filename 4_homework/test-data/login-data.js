export class LoginData {
    constructor(username, password, invalidUsername,invalidPassword) {
        this.username = username;
        this.password = password;
        this.invalidUsername = invalidUsername;
        this.invalidPassword = invalidPassword;
    }
}